import { AppClient } from "../api-client/App.client";
import { InstanceClient } from "../api-client/Instance.client";

export const appClient = new AppClient();
export const instanceClient = new InstanceClient();
